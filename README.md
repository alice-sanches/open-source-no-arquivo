# open-source-no-arquivo
steps to get everything to work:
1. change script execution policy with Set-ExecutionPolicy RemoteSigned -Scope Process 
2. activate virtual environment with .\venv\Scripts\activate